[build-system]
requires = [
    "setuptools >=42",
    "wheel",
    "pybind11>=2.4", 
    "numpy"
]
build-backend = "setuptools.build_meta"

[tool.cibuildwheel]
skip = ["cp36-*", "*-win32", "*-manylinux_i686"]